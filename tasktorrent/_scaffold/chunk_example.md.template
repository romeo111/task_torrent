# Chunk: example-chunk-2026-04-28-1700

> Example chunk-spec generated by `tasktorrent init`.
> Replace this with a real chunk before opening an issue.
> Run `python -m tasktorrent.lint_chunk_spec chunks/example-chunk-2026-04-28-1700.md`
> to validate the schema.

## Status

`queued`

## Topic Labels

`example`, `mechanical+judgment`

## Mission

One paragraph that says exactly what work this chunk does, why it exists, and what's deliberately out of scope. The mission must be specific enough that a contributor can self-assess "is this for me?" without asking the maintainer.

## Economic Profile

```yaml
compute_profile: mixed                # mechanical | llm-essential | mixed
llm_essential_pct: 50

script_alternative:
  exists: partial
  path: null
  rationale: >
    Explain what parts of this work could be a script vs need an LLM.

verification_method: sample            # computational | sample | full-expert | none
verification_cost:
  maintainer_hours: 2
  expert_hours: 0
  expert_specialty: ""

break_even_test: PASS                  # PASS | MARGINAL | FAIL
break_even_rationale: >
  Why a contributor running this chunk is cheaper than a maintainer
  doing it manually, after accounting for review time.

compute_classification: mixed
output_type: entity-sidecar            # entity-sidecar | report-only | mixed
```

## Drop Estimate

~1 Drop (~100k tokens). Replace with real estimate.

## Required Skill

`<skill-name>` — see `task_torrent/skills/`.

## Allowed Sources

List the data sources this chunk may read.

## Disallowed Sources

`SRC-EXAMPLE` (always banned per consumer config — see `.tasktorrent.yaml` `banned_sources`).

## Manifest

List concrete entity IDs or file paths this chunk owns. NO placeholders. Examples:
- `BMA-EGFR-L858R-NSCLC`
- `IND-CRC-1L-RAS-WT`

The contributor commits this list as `contributions/example-chunk-2026-04-28-1700/task_manifest.txt`.

## Computation

Describe the per-entity / per-row work the contributor does. Numbered steps.

## Where computation happens

Contributor's machine. Required: web access? Read access to consumer KB? Etc.

## Re-verification

### Pre-acceptance gates (auto-reject)
- Schema validation on every sidecar.
- ...

### Computational re-verify
- Maintainer runs `scripts/tasktorrent/reverify_<chunk-type>.py`.

### Sample human re-verify (N%)
- Maintainer reads N% random sidecars, confirms ...

### Trust threshold
- Gates: 100%.
- Sample accuracy: ≥ X%.

## Output Format

- `contributions/example-chunk-2026-04-28-1700/<sidecar>.yaml` — one per entity
- `contributions/example-chunk-2026-04-28-1700/task_manifest.txt`
- `contributions/example-chunk-2026-04-28-1700/_contribution_meta.yaml`

## Acceptance Criteria

- All gates pass.
- Sample accuracy ≥ X%.

## Rejection Criteria

- Banned source used.
- Entity IDs outside the manifest.
- Pre-commit hooks bypassed.

## Claim Method

`formal-issue`
